plugins:
  generators:
    rest:
      RestGenerator:
        uri: "http://localhost:8080/generate"
        response_json: true
        response_json_field: "text"
        req_template_json_object:
          prompt: "$INPUT"
